# NewsEviday PRD 变更日志

## v1.2.7 对外文案与架构口径校准 — 2026-08-02

- 公开页面统一使用“最新情报、情报详情、证据问答、关注偏好、质量评测、更新状态、产品与技术”。
- 产品与技术页首屏移除开发阶段、AI 开关、月度预算等内部信息，改为信息覆盖、跨语言整理和证据回链三项用户价值。
- 删除公开页面中的参考项目、私有 GitHub、PRD 和内部进度入口；增加“目标、样例、实测”分离的对外文案规则。
- 趋势简报删除硬编码的用户影响与观察建议；状态页展示实际贡献来源数，不再把成功解析来源数当作内容覆盖。
- 在线 Worker 检索口径从错误的 `chunk_dense` 校正为实际的 `article_dense`；生产级分块检索继续保留在独立 Gate 后。
- 修复中国大陆内容被计入海外、DeepSeek 标题锚点被规范化删除、关注偏好导入校验过宽等问题。
- 增加基础 SEO 元数据、favicon、robots 和《网站文案与技术架构审查报告》；证据问答继续关闭，P7 持久额度和预算保护保持上线前置条件。

## v1.2.6 来源策略确认 — 2026-08-02

- 首批启用来源固定为 9 个：OpenAI、Anthropic、Hugging Face、GitHub AI & ML、Databricks、dbt Labs、arXiv、Qwen、DeepSeek。
- 按产品方向移除 AWS Machine Learning 和 Google DeepMind；基于真实抓取噪声移除 Cloudflare Blog。
- 增加“基础模型与多模态”“AI 产品、Agent 与开发工具”两个主题，保留原有数据中台、Data Agent、统一语义、智能湖仓、RAG/Eval 和元数据治理方向。
- HTML 来源增加官方文章 URL 白名单、标题元素识别和变更日志锚点解析；规则处理由 Python 完成，不增加模型调用。
- 主题匹配改为两次命中饱和计分，避免主题关键词越丰富、单条明确发布越难入选的问题。
- 两次最终真实网络验证均为 9/9 来源成功、40 条内容入选、0 次模型调用；公开网站仍使用演示快照，自动更新保持暂停。

## v1.2.5 实施状态补充 — 2026-08-02

- PRD 需求范围未变化。
- P4–P6 可自行完成的工程基线已实现；真实 DeepSeek、BGE-M3/Vectorize 和人工评测保留为显式 Gate。
- 自动内容更新继续保持可暂停，仓库仅增加手动 GitHub Workflow，不增加定时触发器。
- 公开 Eval 只展示 Demo 工程实测并固定为 `observe`，不替代生产验收。

本文件记录 PRD 的版本、主要调整、决策原因和影响范围。

## 版本规则

- 主版本：产品定位、核心用户或总体架构发生重大变化，例如 `v1.0 → v2.0`。
- 次版本：新增完整功能模块或明显调整产品范围，例如 `v1.0 → v1.1`。
- 修订版本：修正文案、参数、验收标准或非结构性错误，例如 `v1.0 → v1.0.1`。

每次修改 PRD 时，先更新 `NewsEviday-PRD.md` 的版本与日期，再在本文件顶部追加一条记录。

---

## v1.2.5 — 2026-08-02

状态：P3 静态产品完成并通过页面流转与响应式验收。

### 页面与流转

- 8 个公开页面全部从占位实现升级为可浏览产品页，并增加明确的站内 404。
- 文章详情串联信息流、原始来源、证据锚点、相关文章和问答暂停态。
- 趋势简报串联趋势判断、演示证据和文章详情；访客端不提供生成操作。
- 画像页支持本地手动标签、权重、保存、清除、导入和导出，保存后进入可解释推荐视图。
- 产品介绍页补齐产品问题、研究闭环、技术架构、Python/AI 分工、RAG、Eval、成本安全和参考边界。
- Eval 与状态页只展示真实运行状态、方法和目标值，不展示参考项目指标或虚构生产结果。

### 验收基线

- 页面路由使用 URL、锚点和浏览器历史恢复状态，未知路径进入站内 404。
- 360、390、768、1024、1280、1440 六个固定视口覆盖全部 P3 页面，无横向溢出。
- 新增本地画像单元测试和跨页面 Playwright 流程测试。
- 实际实现截图保存至 `design/reference/实现回归/P3-完整页面/`，与活动视觉 reference 分开管理。

## v1.2.4 — 2026-08-02

状态：对齐 P2.5 数据契约与 P3 首页实际实现。

### 数据与页面契约

- 首页统一读取 `/data/current.json`，运行状态统一读取 `/api/status`，移除 `feed.json`、`status.json` 的旧口径。
- “为你推荐”统一使用 `view=recommended`，默认全部情报使用无参数首页路径。
- 静态快照增加演示/生产标识、来源目录和主题目录；AI 整理字段增加“为什么值得看”。
- 演示快照必须在页面显式提示，不将样例内容、计数或推荐结果表述为实时生产数据。

### 实施状态

- P2.5 技术地基已整合进入 `main`，Cloudflare 与 EdgeOne Makers 主备部署事实保留。
- P3 首页按“C 的大画布框架 + A 的信息流组件”进入实现，固定 6 个响应式验收视口。

## v1.2.3 — 2026-08-01

状态：补齐 EdgeOne Makers 备用部署配置和无域名条件下的访问边界。

### 备用部署

- 将 EdgeOne Pages 的现行产品名称统一为 EdgeOne Makers。
- 新增根目录 `edgeone.json`，固定 Vue 构建命令、输出目录、Node 版本、SPA 回退和安全响应头。
- Node 基线调整为 Makers 预装的 24.11.0，npm 支持范围调整为 10–11；本地、GitHub、Cloudflare 与 Makers 共用 lockfile。
- DeepSeek Key 已保存为 Cloudflare Secret，AI 总开关继续关闭，配置密钥不触发模型调用。

### 大陆访问边界

- 中国大陆或全球含大陆加速区的系统预览链接仅 3 小时有效。
- 全球不含大陆加速区的默认项目域名在中国大陆返回 401。
- 当前“不买域名、不备案”的约束下，Makers 定位为备用部署和临时网络验证，不承诺永久稳定的大陆公开入口。
- 稳定大陆访问问题继续保留在 P7 风险清单，交付时准备 Cloudflare 主站、阶段性备用链接、录屏和截图。

### 影响范围

- 不改变功能范围、视觉方向、AI 方案和月度预算。
- README、工程架构、迁移说明和实施计划同步更新。

## v1.2.2 — 2026-08-01

状态：适配 Cloudflare 2026 年新版 Workers Static Assets 部署路径。

### 部署架构

- 主站从原计划的 Cloudflare Pages 调整为 Workers Static Assets。
- Vue 静态资源和 `/api/*` 使用同一个 Worker 部署单元，减少跨域配置和部署对象。
- 新增仓库根目录 `wrangler.jsonc`；`apps/worker/wrangler.jsonc` 保留为 API 独立回滚路径。
- 使用 Workers Builds 连接私有 GitHub 仓库，并由 `main` 分支自动构建发布。
- 云端部署统一使用 `npm run deploy:cloudflare`，在同一命令内完成 Vue 构建和 Wrangler 发布。
- 移除与 Workers SPA 路由冲突的 Pages `_redirects`；EdgeOne 的 SPA 回退改为部署平台配置。
- EdgeOne 继续作为静态备用站，主备范围不变。

### 决策原因

- 当前 Cloudflare 控制台已将新建静态应用入口统一到 Workers。
- Cloudflare 官方 2026 年 7 月迁移指南推荐用 `assets.directory` 和 SPA 显式路由替代 Pages 构建目录配置。
- 同源部署更适合低成本 MVP，静态资源请求不调用 Worker 脚本，只有 `/api/*` 计入 Worker 请求。

### 影响范围

- PRD 技术架构、工程架构、迁移说明和实施计划同步更新。
- 前端功能、页面范围、视觉方案、EdgeOne 备用站和模型成本边界不变。

## v1.2.1 — 2026-08-01

状态：视觉实施规范固定，P1 前端设计准备完成。

### Design Tokens

- 固定 MVP 浅色主题的紫色色阶、语义色、字体、间距、圆角、阴影、布局、控件尺寸、动效和层级。
- 对关键文字、品牌按钮和状态色完成 WCAG AA 对比度校验。
- 增加 `design/tokens/tokens.css` 和 `design/tokens/tokens.json`，作为前端和工具读取的结构化输入。

### 组件与响应式

- 增加基础、情报流、文章证据、RAG、趋势、画像、Eval、状态和产品介绍组件清单。
- 增加 8 个公开页面的组件映射与共用异步/能力状态契约。
- 80 个细分条目作为设计与验收契约，实际 MVP 导出组件限制在 32 个以内，避免 DEMO 过度组件化。
- 固定 640、768、1024、1200、1440px 断点，以及 360、390、768、1024、1280、1440px 六个验收宽度。
- 补齐 8 个公开页面的桌面、平板、手机重排规则；移动端保持完整品牌首屏，正文优先，证据和 Trace 后置或按需展开。
- 首版仅实现浅色主题，深色主题移出 MVP。

### 影响范围

- PRD 进入工程实施准备状态，下一阶段为 P2 工程骨架。
- reference 继续用于整体气质与层级验收；具体数值和响应式行为以活动设计规范为准。

## v1.2 — 2026-07-31

状态：补齐页面级产品设计，作为前端设计与开发输入。

### 页面与信息架构

- 明确核心版包含 8 个公开页面，`/admin` 为增强版。
- 明确顶部导航、画像入口、Eval/状态入口和管理入口规则。
- 将“全部动态”和“为你推荐”定义为首页子视图。
- 增加首页 URL 参数、筛选恢复、滚动恢复和站内/站外跳转规则。
- 为首页、文章详情、情报问答、趋势简报、Eval、状态、产品介绍和画像补充页面目标、入口、去向、模块、状态和信息来源。

### 用户流转

- 增加页面跳转矩阵。
- 增加趋势简报到证据、跨文章问答到引用、产品介绍到技术透明页面的完整流程。
- 增加浏览器返回、URL 状态、滚动位置、本地画像和会话内问题的状态保持规则。
- 统一访客端趋势简报入口为“查看趋势简报”，访客不能直接触发生成。

### 数据来源与产品边界

- 增加页面信息来源和数据血缘矩阵。
- 区分原始事实、Python 确定性加工、AI 生成、用户本地数据和运行数据。
- 明确首页趋势、推荐原因、文章证据、RAG、Eval、状态和产品介绍的数据口径与降级策略。
- 增加页面、流转与信息来源的 MVP 验收标准。

### 视觉与空间整理

- 最终视觉方向固定为“C 的大画布框架 + A 的信息流组件”。
- 四张确认稿成为唯一活动视觉 reference。
- 新建 `design/视觉设计基准.md`。
- 活动 reference 统一到 `design/reference/视觉基准/`。
- 被否决的视觉探索移动到 `archive/视觉探索/2026-07-31/`，仅供追溯。
- 移动端沿用相同视觉风格并采用响应式布局，具体比例和重排在前端设计阶段确定。

## v1.1 — 2026-07-31

状态：完成首轮 PRD 评审，作为视觉与技术实施基线。

### 评审结论

- PRD 有条件通过。
- 主要缺陷是 AI 使用边界偏宽、首版范围偏大、产品介绍页规格不足。
- 参考项目 newnews 未见明确 LICENSE，只复用方法和指标框架。

### 架构调整

- 增加 Python 3.12 离线批处理。
- Python 负责采集、正文提取、标准化、分层去重、规则筛选、快照和 Eval。
- RapidFuzz 与 TF-IDF 负责同语言近重复。
- BGE-M3 只复核跨语言或临界重复，并继续用于 RAG embedding。
- DeepSeek 只处理翻译摘要、模糊分类、画像增强、RAG 回答和趋势简报。
- 内容管道先筛到 30-40 条候选，再调用 DeepSeek。
- 增加 `content-snapshots` 分支与双站快照发布约定。

### 产品调整

- 将“关于项目页”升级为独立 `/product` 产品介绍页。
- 产品介绍页同时展示产品思路、核心能力、整体架构、AI 分工、RAG、Eval、成本与安全。
- 核心版运营配置改为 YAML + GitHub Actions 手动工作流。
- 可视化 `/admin` 调整为增强版，控制 MVP 工期。
- 核心版来源数量收敛为 6-8 个。

### RAG 与评测

- 生产默认采用 chunk-level dense retrieval。
- article-level dense retrieval 保留为 fallback。
- 每篇文章最多注入 2 个 chunk。
- 增加 corpus health gate。
- 增加去重 Precision/Recall 和人工复核率。
- 摘要翻译、趋势简报、Q&A 分别评测。
- hybrid、rerank、Agentic RAG 保持 eval-only。

### 新增文档

- `prd/PRD评审报告-v1.0.md`
- `docs/项目实施计划与进度跟踪.md`
- `archive/视觉探索/2026-07-31/视觉方向设计草稿-v1.0-含历史方案.md`（后续被活动视觉基准替代）

## v1.0 — 2026-07-31

状态：正式需求基线，待视觉设计与开发。

### 新增

- 将正式产品名称确定为 NewsEviday。
- 在 PRD 开头增加完整产品介绍、主要功能和产品亮点。
- 将海内外新闻与技术情报整理确定为核心能力。
- 增加海外原始信息的中文标题、结构化摘要、术语解释和证据保留。
- 增加国内/海外内容比例和信息时效指标。
- 增加 Data Intelligence 一级分类。
- 增加站点主题配置，可以调整 Data Agent、统一语义层、智能数据湖等关注方向。
- 增加可选个人画像和 DeepSeek 语义增强流程。
- 增加画像确认、编辑、导入、导出和清除能力。
- 增加本地标签加权推荐和推荐原因。
- 增加归档、预热、面试三种运行模式。
- 增加内容更新、RAG、简报、画像 AI 和总 AI 开关。
- 增加运营控制台、预算阈值和暂停恢复规则。
- 增加 Cloudflare 主站、EdgeOne 备用静态站的能力边界。
- 增加完整安全、隐私、迁移、上线和验收要求。

### 关键决策

- 默认海外内容占 60%，国内内容占 40%，比例可以调整。
- 站点主题控制每日采集方向，个人画像只调整已发布内容的排序。
- 用户不创建画像时使用通用信息流。
- 个人画像默认保存在浏览器，不建设账号体系。
- MVP 使用标签权重推荐，不训练推荐模型。
- 每位用户不单独采集内容，也不单独生成趋势简报。
- RAG 只检索最近 30 天。
- 默认运行模式为归档模式，模型费用接近零。

### 由历史草稿演进

基于 `archive/Eviday-PRD-v0.1-draft.md` 重新整理。

主要变化：

- 品牌从 Eviday 调整为 NewsEviday；
- 产品范围从通用 AI 情报扩展为海内外跨语言 AI 与数据技术情报；
- 加入站点主题配置和可选个人画像；
- 将成本暂停机制提升为正式产品能力；
- 增加独立 PRD 目录和版本日志。
