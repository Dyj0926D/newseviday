# AI 一周受控试运行

## 目标

验证 DeepSeek V4 Flash 在画像整理、文章结构化增强和引用式问答中的输出稳定性、延迟、费用及安全保护。试运行不追求流量，不自动开放趋势简报。

## 统一安全配置

```text
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_THINKING_ENABLED=false
DEEPSEEK_MAX_RETRIES=0
DAILY_QUESTIONS_PER_IP=2
GLOBAL_DAILY_GENERATIONS=5
SOFT_BUDGET_DAILY_GENERATIONS=3
MAX_CONCURRENT_GENERATIONS=1
MONTHLY_BUDGET_CNY=5
HARD_BUDGET_CNY=10
```

成本估算暂按官方计划峰时价格上限计入：输入 2 元、输出 4 元/百万 Token。实际开放前必须再次核对供应商价格。

## 分阶段开关

| 日期 | 开启能力 | 操作边界 |
| --- | --- | --- |
| 准备日 | 全部关闭 | 部署代码、检查 Secret、测试状态接口 |
| 第 1–2 天 | 文章增强 | 每日最多 3 篇；通过评审 PR 发布，逐篇检查 AI 字段 |
| 第 3–4 天 | 文章增强 | 通过前两日 Gate 后最多 5 篇；仍通过评审 PR 发布 |
| 第 5–6 天 | 文章增强 + 受控 RAG 候选 | 文章最多 5 篇；RAG 只运行指定问题集，趋势简报继续关闭 |
| 第 7 天 | 全部关闭 | 导出聚合统计，复盘质量、错误、延迟和费用 |

## 开启顺序

1. 保持 `RAG_ENABLED=false`、`TREND_BRIEF_ENABLED=false`；
2. 将 `RUNTIME_MODE` 改为 `warmup`；
3. 在线 `AI_ENABLED` 继续保持 `false`，文章增强通过手动工作流独立试运行；
4. 每日检查 D1 用量、Worker 错误率和 DeepSeek 余额；
5. GitHub Actions 每天北京时间 09:17 检查总开关和试运行截止日期；第 1–2 天使用 `max_model_calls=3`，后续通过 Gate 后再人工改为 5；
6. 每日任务采集全部启用来源，按确定性价值分和来源配额筛选；AI 增强名额按来源轮转；
7. RAG 先在固定生产快照上运行评测；证据门禁、人工黄金集和回答引用同时通过前，不向所有访客开放；
8. 任一停止条件触发时，将 `DAILY_REFRESH_ENABLED=false`，在线生成保持关闭。

## 停止条件

- 月累计费用达到 5 元软预算或 10 元硬上限；
- 出现密钥泄漏、额度绕过或同一请求重复计费；
- 错误率超过 10%；
- 画像结构化成功率低于 90%；
- 文章结构化成功率低于 90%；
- 指定无答案问题中少于 4/5 被正确拒答；
- RAG 引用无法打开、引用与回答明显不一致；
- p95 端到端延迟持续超过 20 秒。

## 试运行记录

只记录聚合数据：日期、功能、请求数、成功数、拒答数、错误类型、p50/p95、Token 和费用。默认不导出用户原始问题或画像文本。

## 回滚

```text
RUNTIME_MODE=archive
AI_ENABLED=false
INGESTION_ENABLED=false
RAG_ENABLED=false
TREND_BRIEF_ENABLED=false
```

回滚后静态站和最后一份内容快照继续可用。

## 当前进展（2026-08-06）

- Day 1 已采集 9 个来源，7 个来源实际贡献 40 条内容；DeepSeek V4 Flash 完成 3 篇结构化整理；
- Day 1 生产快照已进入发布分支，并建立不可变历史版本；
- 24 题生产试运行集已运行：Recall@5 0.9167、Hit@5 1.0、MRR 0.7625、NDCG@10 0.7735、本地 p95 26 ms；
- 旧单阈值拒答无答案识别为 0.25；加入范围、时间和必需证据检查后，`bounded_v1` 证据门禁达到工程草稿线。黄金题和回答引用仍待人工复核，因此生产 Gate 未通过；
- Day 1 内容结构偏向基础模型、RAG 和开发工具；已扩充数据平台关键词、降低通用模型权重。无模型探针运行后数据平台从 0 增至 3 篇、湖仓从 0 增至 1 篇，但 Data Agent 与语义层仍为 0，继续作为来源优化项；
- Day 2 已完成采集与人工发布复核：9 个来源中 8 个实际贡献内容，保留 40 条；当前快照共有 5 篇 AI 结构化内容，其中 2 篇为本日新增；
- 一篇 Databricks 内容因来源摘要不足 120 字、模型生成了来源未明确支持的产品细节而被撤回 AI 解读，仅保留原始标题、摘要和来源链接；流水线已增加“证据文本少于 120 字不调用模型”的前置门禁；
- 数据平台 3 篇、湖仓 1 篇；当前内容缺口缩小为 Data Agent、元数据治理和语义层 3 类；
- Day 3 是否将每日 AI 增强上限从 3 篇提升到 5 篇，继续以内容证据质量、运行错误和费用为准。在线 RAG 仍保持关闭。
